# NIVORA V59 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn V58 into a versioned, reproducible and historically gradable V59 decision platform without replacing the thesis-first engine.

**Architecture:** Keep `lib/nivora-investor.ts` canonical, add isolated V59 policy/snapshot/arena/provider modules, integrate version constants into APIs/UI, and persist point-in-time validation records in Supabase. Tests compile engine-only TypeScript with a dedicated tsconfig so validation is independent from the Next.js UI build.

**Tech Stack:** Next.js 15, React 19, TypeScript, Node test runner, Supabase/PostgreSQL.

**Spec:** `docs/superpowers/specs/2026-09-01-nivora-v59-design.md`

## Global Constraints
- Preserve V58 thesis-first behavior and missing-data neutrality.
- Engine version is `v59`.
- Weights version is `v59-thesis-1`.
- Valuation version is `v59-archetype-3`.
- Today policy version is `v59-today-1`.
- No autonomous live execution.
- No secrets in the release package.

---

### Task 1: Canonical version and Today policy
**Files:** Create `lib/nivora-version.ts`, `lib/nivora-today.ts`; modify `lib/nivora-investor.ts`; test `tests/v59-platform.test.mjs`.
- [ ] Write failing Today/version tests.
- [ ] Verify failure.
- [ ] Implement canonical versions and deterministic Today policy with veto precedence.
- [ ] Verify tests pass.

### Task 2: Frozen decision snapshots
**Files:** Create `lib/nivora-snapshot.ts`; test `tests/v59-platform.test.mjs`.
- [ ] Write failing deterministic fingerprint tests.
- [ ] Verify failure.
- [ ] Implement stable canonical serialization and fingerprint.
- [ ] Verify tests pass.

### Task 3: Arena and reliability
**Files:** Create `lib/nivora-arena.ts`; test `tests/v59-platform.test.mjs`.
- [ ] Write failing raw/relative outcome and sample-gate tests.
- [ ] Verify failure.
- [ ] Implement horizon grading and calibration summary.
- [ ] Verify tests pass.

### Task 4: Provider resilience
**Files:** Create `lib/provider-resilience.ts`; modify `lib/shared-cache.ts`, `lib/provider-registry.ts`; test `tests/v59-provider.test.mjs`.
- [ ] Write failing coalescing/health tests.
- [ ] Verify failure.
- [ ] Implement in-flight request coalescing and provider health state.
- [ ] Verify tests pass.

### Task 5: Persistence and product integration
**Files:** Create `supabase/migrations/20260901_nivora_v59_arena.sql`; modify validation/health/calibration APIs, methodology and StockClient.
- [ ] Add V59 snapshot/outcome/reliability schema.
- [ ] Switch V57 hard-coded runtime metadata to V59 constants/semantics.
- [ ] Persist V59 snapshot metadata including Today policy and evidence fingerprint.
- [ ] Keep calibration collecting until minimum evidence exists.

### Task 6: Release verification and packaging
**Files:** Modify `package.json`, add `tsconfig.engine.json`, create `V59_VALIDATED_DECISION_PLATFORM_RELEASE.md`.
- [ ] Run V54/V56/V57/V57.2/V58/V59 engine regressions.
- [ ] Run TypeScript/Next build when dependencies are available.
- [ ] Package source without `.env.local`, build cache, dependencies, git metadata or macOS files.
