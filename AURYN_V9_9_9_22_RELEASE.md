# AURYN V9.9.9.22 — Multi-provider Current Price
V21 diagnostics finally establish the external provider state: Alpaca is ~16 hours stale; Twelve `/quote` returns an extended observation but it is 41–232 minutes old; Twelve 1-minute pre/post endpoint is HTTP 403 because the connected plan lacks pre/post entitlement. AURYN must not label those values current. V22 adds the already-configured Finnhub quote provider as a third timestamped candidate for current-price authority. It also corrects diagnostics so stale Twelve extended observations are no longer described as fresh. If Finnhub is also stale/unavailable, AURYN intentionally remains unavailable rather than fabricate a current price.
