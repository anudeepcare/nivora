import {formatMoney,formatPercent} from "@/lib/nivora-format";
export default function StockSecurityHeader({company,symbol,price,changePct,status,detail,owns=false,positionLoaded=false,onToggleOwn}:{company?:string;symbol:string;price:number;changePct:number;status:string;detail:string;owns?:boolean;positionLoaded?:boolean;onToggleOwn?:()=>void}){
 return <><header className="aurynStockHeader"><div><small>{company||symbol}</small><h1>{symbol}</h1>{onToggleOwn&&<button type="button" className={`aurynOwnChip ${owns?"on":""}`} onClick={onToggleOwn}>{owns?(positionLoaded?"✓ Position loaded":"✓ I own this"):"+ I own this"}</button>}</div><div className="aurynStockQuote"><b>{formatMoney(price)}</b><span className={changePct>=0?"up":"down"}>{formatPercent(changePct)}</span></div></header><div className="aurynStockFresh"><span>{status}</span><span>{detail}</span></div></>
}
