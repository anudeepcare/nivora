import {NextResponse} from "next/server";import {ema,pct,rnd} from "@/lib/quant";
async function one(symbol:string,key:string){const u=`https://api.twelvedata.com/time_series?symbol=${encodeURIComponent(symbol)}&interval=1day&outputsize=80&apikey=${key}`;const j=await fetch(u,{next:{revalidate:600}}).then(r=>r.json());if(!j.values)return null;const r=j.values.slice().reverse(),c=r.map((x:any)=>+x.close),p=c.at(-1)!,e20=ema(c,20).at(-1)!,e50=ema(c,50).at(-1)!;const score=(p>e20?1:-1)+(e20>e50?1:-1);return{symbol,price:rnd(p),changePct:rnd(pct(p,c.at(-2)??p)),trend:score>=2?"Strong":score<=-2?"Weak":"Mixed"}}
export async function GET(){const key=process.env.TWELVE_DATA_API_KEY;if(!key)return NextResponse.json({items:[],regime:"Unavailable"});try{const items=(await Promise.all([one("SPY",key),one("QQQ",key),one("IWM",key),one("BTC/USD",key)])).filter(Boolean);const equity=items.filter((x:any)=>["SPY","QQQ","IWM"].includes(x.symbol));const strong=equity.filter((x:any)=>x.trend==="Strong").length,weak=equity.filter((x:any)=>x.trend==="Weak").length;const regime=strong>=2?"Risk-on":weak>=2?"Risk-off":"Mixed";return NextResponse.json({items,regime})}catch{return NextResponse.json({items:[],regime:"Unavailable"})}}
