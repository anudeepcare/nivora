"use client";import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {supabaseBrowser} from "@/lib/supabase";
export default function Register(){const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false),r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const {error}=await supabaseBrowser().auth.signUp({email,password,options:{data:{full_name:name}}});setBusy(false);if(error)setMsg(error.message);else r.replace("/dashboard")}return <main className="osAuth"><Link className="osLogo" href="/">NIVORA<span>.</span></Link><form onSubmit={submit}><small>GET STARTED</small><h1>Create account</h1><p>One private workspace for your watchlist, alerts and market decisions.</p><label>Name<input autoFocus autoComplete="name" value={name} onChange={e=>setName(e.target.value)} required/></label><label>Email<input autoComplete="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input autoComplete="new-password" minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={busy}>{busy?"Creating…":"Create account"}</button>{msg&&<div className="formError">{msg}</div>}<div className="osAuthSwitch">Already have an account? <Link href="/login">Sign in</Link></div></form></main>}
