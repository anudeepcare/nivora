"use client";import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {supabaseBrowser} from "@/lib/supabase";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false),r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const {error}=await supabaseBrowser().auth.signInWithPassword({email,password});setBusy(false);if(error)setMsg(error.message);else r.replace("/dashboard")}return <main className="osAuth"><Link className="osLogo" href="/">NIVORA<span>.</span></Link><form onSubmit={submit}><small>WELCOME BACK</small><h1>Sign in</h1><p>Your watchlist, alerts and decisions in one place.</p><label>Email<input autoFocus autoComplete="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input autoComplete="current-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={busy}>{busy?"Signing in…":"Continue"}</button>{msg&&<div className="formError">{msg}</div>}<div className="osAuthSwitch">New to NIVORA? <Link href="/register">Create account</Link></div></form></main>}
