"use client";import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {supabaseBrowser} from "@/lib/supabase";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false),r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");try{const result:any=await Promise.race([supabaseBrowser().auth.signInWithPassword({email,password}),new Promise((_,reject)=>setTimeout(()=>reject(new Error("Sign in is taking too long. Check your connection and try again.")),12000))]);setBusy(false);if(result.error)setMsg(result.error.message);else{r.replace("/dashboard");r.refresh()}}catch(e:any){setBusy(false);setMsg(e?.message||"Unable to sign in right now.")}}return <main className="osAuth"><Link className="osLogo" href="/">NIVORA<span>.</span></Link><form onSubmit={submit}><small>WELCOME BACK</small><h1>Sign in</h1><p>Your watchlist, alerts and decisions in one place.</p><label>Email<input autoFocus autoComplete="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input autoComplete="current-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={busy}>{busy?"Signing in…":"Continue"}</button>{msg&&<div className="formError">{msg}</div>}<div className="osAuthSwitch">New to NIVORA? <Link href="/register">Create account</Link></div></form></main>}
